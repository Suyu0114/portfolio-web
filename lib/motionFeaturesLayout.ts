// Motion's features plus layout animation, imported on demand by
// FacetFilter only, so no page but /projects downloads them (SPEC §4.4,
// v1.11).
import { domMax } from "motion/react";

export default domMax;
