// Motion's animation features, imported on demand by MotionProvider so they
// load in their own chunk after hydration (SPEC §4.4, v1.11).
import { domAnimation } from "motion/react";

export default domAnimation;
