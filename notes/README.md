# notes/ — private source material (not published, not committed)

Drop raw material here for drafting case studies in P3:

- each project's README / claude.md / architecture notes
- verified numbers, dates, links per project
- CV drafts (the final `resume.pdf` goes to `/public` at P5)

Everything in this folder except this README is gitignored. Nothing
here is read by the build or shipped to the site — it exists so Claude
can reference real project facts instead of inventing them
(CLAUDE.md rule 1).
